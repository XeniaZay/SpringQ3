// Корневой build.gradle — общие настройки для всех подпроектов
allprojects {
    group = 'com.FromPegaToJava'
    version = '0.0.1-SNAPSHOT'
}

subprojects {
    repositories {
        mavenCentral()
    }
}